[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hybrid-bp-nsg-channel-aligned"
version = "14.1.0"
description = "Standalone v14.1 residual AI/Tanner-GRAND rescue decoder for 5G-style LDPC"
requires-python = ">=3.10"
authors = [{name="Hybrid GRAND v14"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["hybrid_bp_nsg*"]
